# 한 거리의 300년 — OpenDesign 구현 계획

> 작성일: 2026-09-14  
> 상태: 구현 승인 전 계획 v0.1  
> 근거 문서: `00-shared-design-principles.md`, `09-street-through-centuries.md`  
> 적용 방향: OpenDesign `web-prototype` + `warm-editorial`

## 1. 이번 단계의 결론

첫 결과물은 실제 종로 구간을 복원한 역사 콘텐츠가 아니라, 합성된 **연습 거리**와 **목업 사료**로 학습 흐름을 검증하는 밝은 테마의 단일 페이지 프로토타입으로 만든다.

현재 문서만으로는 앱 구현이 승인되지 않았으므로 이 계획에서는 코드, 이미지 생성, 커밋, 푸시, 배포를 수행하지 않는다. 구현 승인 뒤에도 실제 사료 검수 전에는 특정 상점·건물·간판을 역사 사실처럼 확정하지 않는다.

## 2. 학습 핵심

학생은 같은 관찰 지점을 `1725 → 1875 → 2025`로 비교하고 다음 네 종류를 구분한다.

| 분류 | 화면에서의 의미 | 판정 경계 |
|---|---|---|
| 관찰 | 장면에서 직접 보이는 요소 | 역사 사실로 자동 승격하지 않음 |
| 역사 사실 | 검수된 사료가 직접 확인하는 범위 | 날짜·장소·검수 상태가 모두 맞아야 함 |
| 추론 | 관찰과 사료를 연결한 해석 | 근거 링크는 확인하되 자유 문장 진실성은 자동 판정하지 않음 |
| 모름 | 자료가 부족해 결론을 보류한 상태 | 실패가 아니라 적절한 탐구 결과로 인정 |

최소 성취 증거는 관찰 지점 4개 이상, 유효 근거 연결 3개 이상, 설명 수정 1회다.

## 3. 화면 구조

OpenDesign 레이아웃을 아래 순서로 조합한다.

1. **분할형 도입** — 제품 문장, 3개 시점, 현재 할 일, `탐구 시작` 버튼.
2. **시점 비교 띠** — A·B·C 라벨과 같은 기준점의 변화. 자동 재생 없이 탭으로 전환.
3. **탐구 작업대** — 왼쪽 장면/평면도, 오른쪽 관찰 지점·사료·분류·설명 패널.
4. **성취 근거** — 관찰 수, 유효 근거 수, 수정 횟수를 점수가 아닌 과정 기록으로 표시.
5. **다음 단계 안내** — 새 단서를 열고 설명을 수정하는 한 개의 필수 행동.

상단에는 시점 탭, 현재 과제, 작은 `업데이트 내역` 버튼을 둔다. 920px 이하에서는 열을 하나로 줄이고, 360px 이하에서는 `장면 → 사료 → 답안` 순서를 유지한다.

## 4. 시각 언어

- OpenDesign `warm-editorial`의 따뜻한 종이 배경, 짙은 갈색 계열 본문, 테라코타 단일 강조색, 세리프 제목을 적용한다.
- 시점은 색만으로 구분하지 않고 `A·1725`, `B·1875`, `C·2025` 라벨과 아이콘을 함께 쓴다.
- 한 화면에서 현재 필수 버튼 하나에만 `gi-pulse`를 적용한다. `prefers-reduced-motion`에서는 정적인 강조 테두리로 바꾼다.
- 역사 재구성 자산에는 항상 `사료 기반 시각 재구성 · 실제 사료 사진 아님` 캡션을 둔다.
- 검수 전 프로토타입은 OpenDesign 플레이스홀더 또는 명확히 표시한 합성 연습 장면을 사용한다.
- 밝은 테마만 제공하며 운영체제 다크 모드로 자동 전환하지 않는다.

## 5. 프로토타입 상호작용

### 5.1 시점과 관찰 지점

- 시점 탭을 바꾸면 장면, 선택 지점, 사료 패널의 시대가 함께 갱신된다.
- P01~P06은 버튼 목록과 장면의 위치 표식을 모두 제공한다.
- 키보드 사용자는 탭과 기본 버튼 순서만으로 같은 흐름을 완료할 수 있다.

### 5.2 주장 분류

- 검수된 카드 문장을 `관찰 / 역사 사실 / 추론 / 모름`으로 분류한다.
- 생성 장면 메타데이터만 선택한 경우 `역사 사실` 연결을 허용하지 않는다.
- 자유 서술은 저장·비교만 하며 자동으로 역사적 진실 여부를 평가하지 않는다.

### 5.3 사료 연결

- 목업 사료 카드는 `sourceId`, 날짜, 장소 범위, 자료 종류, 검수 상태를 가진다.
- `S07(1896)` 또는 `S08(1898)`을 `1875` 장면의 직접 근거로 선택하면 시간축 경고를 표시하고 자동 확정하지 않는다.
- 관련은 있지만 동일 구간이 아닌 자료는 `참고 사례`로 분리한다.

### 5.4 설명 수정

- 학생은 원인 2개, 결과 1개, 불확실성 1개를 선택해 설명 초안을 만든다.
- 새 단서 공개 뒤 수정 전·후를 현재 세션에서 나란히 비교한다.
- 계정, 영구 저장, 경쟁 점수, 런타임 AI 호출은 넣지 않는다.

## 6. 상태와 엔진 경계

```text
SessionState
├─ selectedEra: 1725 | 1875 | 2025
├─ selectedPoint: P01..P06
├─ evidenceLinks[]
├─ claims[]
├─ completedTasks[]
└─ revisionCount

검수 엔진
├─ 시대 일치 검사
├─ 장소 범위 검사
├─ reviewStatus 검사
├─ 직접/간접/참고 근거 분리
└─ 성취 증거 집계
```

엔진은 구조화된 ID와 메타데이터만 검증한다. 렌더링 효과나 생성 이미지의 픽셀을 사실 판정에 사용하지 않는다.

## 7. 예상 파일 구성

OpenDesign 시안 단계는 단일 `index.html`로 시작하되, 코드가 500줄에 가까워지거나 학습 로직이 확정되면 다음 모듈 구조로 전환한다.

```text
index.html
src/
├─ domain/session-state.ts
├─ engine/evidence-validator.ts
├─ content/eras.ts
├─ content/points.ts
├─ content/mock-sources.ts
├─ components/timeline-tabs.ts
├─ components/inquiry-workbench.ts
├─ components/update-history.ts
└─ styles/app.css
tests/
├─ evidence-validator.test.ts
└─ learner-flow.test.ts
```

한 파일은 500줄을 넘기지 않는다. 콘텐츠와 판정 엔진, 화면 표현을 서로 분리한다.

## 8. 자산 제작 계획

이미지 생성은 별도 구현 승인 뒤 수행한다.

1. 먼저 동일 카메라·도로 축·기준점이 유지되는 합성 연습 장면 3종을 만든다.
2. 자산마다 `assetId`, `eraId`, `role`, `invariant`, `variant`, `caption`, `reviewStatus`를 기록한다.
3. 텍스트, 간판, 특정 인물·상호는 임의로 생성하지 않는다.
4. 실제 사료 카드와 합성 재구성 이미지는 시각적 라벨과 메타데이터 모두에서 분리한다.
5. 이미지 생성 도구가 세부 모델을 노출하지 않으면 특정 모델 사용을 확인했다고 기록하지 않는다.

## 9. 검증 계획

### 엔진 검증

- 생성 이미지 선택은 `observation`만 만들고 `historicalFact`를 만들지 않는다.
- 1896·1898 자료를 1875 직접 근거로 연결하면 경고가 발생한다.
- 장소 범위 또는 검수 상태가 맞지 않으면 역사 사실 연결이 유효하지 않다.
- 이미지 실패 시 캡션·사료 텍스트·대체 평면도·sourceIds가 남는다.

### 학생 흐름 검증

- 예측 → 관찰 → 사료 연결 → 분류 → 비교 설명 → 새 단서로 수정까지 실제로 수행한다.
- 다음 필수 행동 하나만 `gi-pulse`로 강조되는지 확인한다.
- 잘못된 근거 연결 뒤 날짜·범위 단서를 보고 수정할 수 있는지 확인한다.

### 화면 검증

- 1280px, 360px, 320px에서 가로 넘침과 잘림을 확인한다.
- 키보드 이동, 초점 표시, 200% 확대, 모션 감소를 확인한다.
- VoiceOver 구현 및 검증은 범위에서 제외한다.

### 증거 수준 구분

- 정적 검사 통과
- 로컬 브라우저 학생 흐름 통과
- 공개 배포 확인
- 역사·교과 검수 완료

각 단계는 별도로 보고하며 앞 단계 통과를 뒤 단계의 증거로 과장하지 않는다.

## 10. 구현 순서

1. OpenDesign seed와 `warm-editorial` 토큰으로 기본 화면을 구성한다.
2. 목업 콘텐츠와 세션 상태를 연결한다.
3. 독립 근거 검증 엔진과 단위 테스트를 작성한다.
4. 네 과제의 최소 상호작용을 구현한다.
5. 업데이트 내역, gi-pulse, 모션 감소, 오류 대체 화면을 추가한다.
6. 320/360/1280px 및 키보드 학생 흐름을 검증한다.
7. 승인된 경우에만 실제 자산 생성·사료 교체·커밋·푸시·배포를 각각 진행한다.

## 11. 구현 착수 전 확인 항목

- [ ] 이 계획 기준의 로컬 프로토타입 구현 승인
- [ ] 0단계에서 합성 연습 거리와 목업 사료 사용 승인
- [ ] 이미지 생성 범위 승인
- [ ] 실제 종로 구간·사료 검수는 후속 단계임을 확인
- [ ] 커밋·푸시·배포·HVC 등록·정적 갤러리 동기화는 각각 별도 승인

## 12. 변경 이력

| 날짜 | 버전 | 내용 |
|---|---|---|
| 2026-09-14 | 0.1 | 제공 문서와 OpenDesign 규칙을 결합한 구현 전 계획 작성 |
